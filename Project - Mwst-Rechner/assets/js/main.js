switch getCalcVariation(){
    const calcVariation = getElementById(nettoZuBrutto);
    let output = let step1


    switch (calcVariation) {
        if 
    }
}


















// Netto zu Brutto
// Aufschlag
// input * 1.19
// input * 1.07

    // Brutto zu Netto
    // Abschlag
    // (input / 100) * 19 = A
    // (input - A) = output

    // (input / 100) * 7 = A
    // (input - A) = output